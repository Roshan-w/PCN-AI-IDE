all:
  children:
    master:
      hosts:
        master1:
          ansible_host: 10.0.0.1
    workers:
      hosts:
        worker1:
          ansible_host: 10.0.1.1
        worker2:
          ansible_host: 10.0.1.2
        # ... up to worker99
